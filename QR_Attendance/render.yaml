services:
  - type: web
    name: qr-attendance
    runtime: go
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: PORT
        value: 8080
      - key: FIREBASE_CONFIG
        sync: false
    healthCheckPath: /
    autoDeploy: true
